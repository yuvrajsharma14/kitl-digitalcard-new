{
  "name": "@my-digital-card/mobile",
  "version": "0.0.1",
  "private": true,
  "description": "My Digital Card — React Native + Expo mobile app (Phase 2)",
  "scripts": {
    "dev": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "build": "echo 'Mobile build: Phase 2'"
  },
  "dependencies": {
    "@my-digital-card/shared": "workspace:*"
  }
}
